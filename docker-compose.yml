services:
  cv-app:
    build: .            # build from the local Dockerfile (nginx serving index.html)
    container_name: cv-app
    restart: always
    networks:
      - ai_stack_git_edge   # the live edge network cloudflared reaches; NEVER openclaw_default (dead — caused the 2026-07-11 cv 502)

networks:
  ai_stack_git_edge:
    external: true
